# SYNDI x402 — Environment Variables
# Copy to .env and fill in values: cp .env.example .env

# ── Required ──
# OpenAI API key for LLM-powered debates and evaluation
OPENAI_API_KEY=sk-...

# 24-word mnemonic from which all 18 agent wallets are derived
# Generate one with: npm run create-wallets
STACKS_MASTER_MNEMONIC=

# ── Optional ──
# Network: testnet (default) or mainnet
STACKS_NETWORK=testnet

# Server port (default: 3402)
PORT=3402

# Stacks API URL (default: https://api.testnet.hiro.so)
# STACKS_API_URL=https://api.testnet.hiro.so

# OpenAI vector store for RAG (optional, enhances Syndi's responses)
# SYNDI_VECTOR_STORE_ID=vs_...
